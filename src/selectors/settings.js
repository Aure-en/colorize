export const getFormat = (state) => state.settings.format;

export const getCreationPage = (state) => state.settings.creationPage;

export const getIsDarkMode = (state) => state.settings.isDarkMode;

export const getSortBy = (state) => state.settings.sortBy;

export const getFilterBy = (state) => state.settings.filterBy;
